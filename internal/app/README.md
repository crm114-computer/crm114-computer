# internal/app

Core Go packages for CRM114 live here. The package currently exposes a simple `Greeting()` helper used to validate the Go toolchain and TDD workflow.

Keep this README updated whenever functionality in this directory changes so repository browsers explain the role of these Go modules.
